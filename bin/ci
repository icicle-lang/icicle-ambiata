#!/bin/sh -exu

$(dirname $0)/ci.branches


VERSION=$(cat $(dirname $0)/../dist/build/autogen/version.txt)
aws s3 --region=ap-southeast-2 cp dist/build/icicle/icicle s3://ambiata-dist/icicle/linux/x86_64/$VERSION/icicle-$VERSION
