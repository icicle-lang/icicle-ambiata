-- Divisions return Possibles.
-- Try with group:
feature salary ~> group (value / 200) ~> sum (value / 5) / 5

-- Distincts
feature salary ~> distinct (value / 5) ~> sum (value / 5) / 5

-- Maybe some lets
feature salary ~> let x = 2 / 1 ~> sum (value * x)

-- Add a filter
feature salary ~> let x = 2 / 1 ~> filter (value > x) ~> count

-- newest or something
feature salary ~> (newest value / 5, oldest (value / 5), sum value)

