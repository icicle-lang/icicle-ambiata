-- Show everything
:set +flatten +c

-- Enable C evaluation
:set +c-eval

-- An interesting expression with structs and strings
feature injury ~> mean (double severity) * (filter location == "torso" ~> sd severity)

-- Dates
feature salary ~> max (days between `1980-01-06` date days after `2000-01-01`)
