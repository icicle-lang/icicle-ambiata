-- Try a simple sum. Expect 1500 and 30.
feature salary ~> sum value

-- Tuples
feature salary ~> (sum value, count)

-- Now let's try average. Expect 300 and 10.
feature salary ~> double (sum value) / count

-- Filtering
feature salary ~> filter value > 10 ~> count

-- Average of latest 3 entries
feature salary ~> latest 3 ~> sum (double value) / count

-- Now should collect the current date
feature salary ~> now

-- Standard deviation should be correct
feature salary ~> sd (double value)

-- Gradient should be correct
feature salary ~> gradient (double value) (double (days date) / 365)
